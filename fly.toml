# fly.toml app configuration file generated for product-management-dark-paper-6559 on 2025-11-03T13:45:42Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'product-management-dark-paper-6559'
primary_region = 'cdg'

[build]
  image = "ghcr.io/${{ github.repository }}:latest"  # Or use env vars/secrets for dynamic tags

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
